# Directory student quiz history

In the School Directory, Student section (`/admin/directory?tab=students` and `/admin/directory/students`), add a button on each student. When clicked, show all quiz history for that particular student.
