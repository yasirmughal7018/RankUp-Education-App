/**
 * App design-system query keys.
 * Prefer extending `@/core/api/queryKeys` for feature caches already in use.
 */
export { queryKeys as coreQueryKeys } from "@/core/api/queryKeys";
export { queryKeys } from "@/core/api/queryKeys";
