<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Debug builds allow plain HTTP to the local API on the emulator. -->
    <application android:usesCleartextTraffic="true" />
</manifest>
